<%= semantic_form_for @task do |f| %>

  <%= f.input :title %>   
  <%= f.input :body, :as => :text %>   
   
  <%= f.action :submit, :as => :button %>
  <%= f.action :cancel, :as => :link %>
  
<% end %>
